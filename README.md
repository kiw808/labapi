# Lab API

A sandbox Node.js API.

## Setup

- Clone the project
- `cd` to the project directory
- Install dependencies
  ```
  npm install
  ```
- Start the development server
  ```
  make start
  ```
